<% if current_user.following?(user) %>
  <%= link_to "フォロー解除", user_relationships_path(user.id), class:"btn btn-danger rounded-pill btn-block btn-sm", method: :delete, remote: true %><br>
<% else %>
  <%= link_to "フォロー", user_relationships_path(user.id), class:"btn btn-primary rounded-pill btn-block btn-sm", method: :post, remote: true %><br>
<% end %>


